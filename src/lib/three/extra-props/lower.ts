import type { ExtraPropBuilder } from "./types";

/** Props for the lower-body exercise library, keyed by `kind` (use a "lower:" prefix). */
export const LOWER_PROPS: Record<string, ExtraPropBuilder> = {};
